value(life).
value(freedom).


/*

It would be nice to have the following definitions:

% subscribes means an agent has an interest into a value
subscribes(hal, life).
subscribes(hal, freedom).
subscribes(carla, life).
subscribes(carla, freedom).

% means that an agent's attribute effects directily the stauts of a value 
effects(alive, life).
effects(money, freedom).
effects(shops, freedom).

*/
